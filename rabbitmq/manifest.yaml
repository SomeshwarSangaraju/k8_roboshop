apiVersion: apps/v1
kind: Deployment
metadata:
  name: rabbitmq
  namespace: roboshop
  labels:
    project: roboshop
    component: rabbitmq
    environment: dev
spec:
  replicas: 3
  selector:
    matchLabels:
      project: roboshop
      component: rabbitmq
      environment: dev
  template:
    metadata:
      name: rabbitmq
      namespace: roboshop
      labels:
        project: roboshop
        component: rabbitmq
        environment: dev
    spec:
      containers:
      - name: rabbitmq
        image: someshwarsangaraju/rabbitmq:v1
        imagePullPolicy: Always
        env:
        - name: RABBITMQ_DEFAULT_USER
          value: roboshop
        - name: RABBITMQ_DEFAULT_PASS
          value: roboshop123
        readinessProbe:
          tcpSocket:
            port: 5672 
          initialDelaySeconds: 10
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: 5672 
          initialDelaySeconds: 5
          periodSeconds: 5
        # ports:
        # - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: roboshop
spec:
  selector:
    project: roboshop
    component: rabbitmq
    environment: dev
  ports:
  - protocol: TCP
    port: 5672 
    targetPort: 5672 

